/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  // add any of your own custom config here
  eslint: {
    ignoreDuringBuilds: true,
  },
};

export default nextConfig;
